[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "wiener-poc"
version = "0.1.0"
description = "PoC: Rust + CUDA + Python Wiener Filter"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
]
